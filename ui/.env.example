# Leave VITE_AZURE_TENANT_ID unset to enable dev-mode (no auth required)
VITE_AZURE_TENANT_ID=
VITE_AZURE_CLIENT_ID=

# API base URL — defaults to /api (proxied by Vite dev server to localhost:8000)
# Set this for production deployments
VITE_API_BASE_URL=
